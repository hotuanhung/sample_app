# config/application.yml

client:
  host: "localhost:3000"
validate:
  email:
    length:
      max: 255
    regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  name:
    length:
      max: 50
  password:
    length:
      min: 3
page_30: 30
gravatar_edit_link: "https://gravatar.com/emails"
